# Диагностика веб-сервера Nginx

## Цель проекта
Научиться работать с логами Linux, диагностировать и устранять проблемы с веб-сервисами.

## Что сделал

### 1. Установка Nginx
```bash
sudo apt update
sudo apt install nginx
sudo systemctl start nginx
sudo systemctl status nginx
